// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for AddTenantFunction
 */
export interface AddTenantFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/ddb-stream/add-tenant.
 */
export class AddTenantFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: AddTenantFunctionProps) {
    super(scope, id, {
      description: 'src/ddb-stream/add-tenant.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/ddb-stream/add-tenant.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}